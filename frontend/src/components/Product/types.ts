import { LucideIcon } from "lucide-react";

export interface ProductCategory {
    title: string;
    desc: string;
    image: string;
    icon: LucideIcon;
    buttonText: string;
    link: string;
}
